//! Placeholder example. The `kyberlib-asm` crate is a skeleton
//! (`publish = false`) — the SIMD acceleration surface ships from
//! `crates/kyberlib/src/avx2/` until issue #143 (the file-level
//! relocation) lands. See the crate's `lib.rs` doc-block for
//! the current state.

fn main() {
    println!("kyberlib-asm is currently a skeleton crate; see lib.rs.");
}
