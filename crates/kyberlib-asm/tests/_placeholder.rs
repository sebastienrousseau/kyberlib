//! Placeholder integration test. The `kyberlib-asm` crate exposes
//! no public API yet; this file exists so the `tests/` directory
//! is real in git and the layout matches the noyalib reference.

#[test]
fn crate_compiles() {
    // Touch the crate's only public item — its module-level docs.
    // Compilation here is the test.
}
