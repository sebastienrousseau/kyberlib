//! Placeholder example. `kyberlib-pkcs8` is a skeleton
//! (`publish = false`); the PKCS#8 / SPKI / PEM encoding surface
//! lands in a future release. See the crate's `lib.rs`.

fn main() {
    println!("kyberlib-pkcs8 is currently a skeleton crate.");
}
