//! Placeholder example. `kyberlib-wasm` is a cdylib wrapper;
//! consumers run the JS bindings from a browser/wasm host rather
//! than `cargo run --example`. See `pkg/` for the wasm-bindgen
//! distribution layout.

fn main() {
    println!("kyberlib-wasm targets wasm32-unknown-unknown; see pkg/ for the JS bindings.");
}
